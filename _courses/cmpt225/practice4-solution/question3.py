"""
write an algorithm that gets a node in a binary tree
and returns its depth (distance from the root) in O(depth) time.
"""
from binary_tree import Node, BinaryTree, in_order, build_tree1, build_tree2, build_tree3


# TODO